"""
Hardware Abstraction Layer (HAL) subsystem.
 
Modules:
    base    — abstract GimbalHALBase interface (all flight HALs must subclass this)
    gimbal  — SimulatedGimbalHAL for SITL testing (flight HAL: TBD pending hardware selection)
"""
