"""
Fault detection subsystem.
 
Modules:
    detector — FaultDetector; must be called every cycle to check encoder health,
               watchdog, and over-temperature (REQ-CTRL-FAULT-001 through 003)
"""
